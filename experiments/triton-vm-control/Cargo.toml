[package]
name = "triton-vm-control"
version = "0.0.0"
edition = "2024"
publish = false

# This is an isolated feasibility benchmark. It is deliberately not a member
# of the production workspace and must never be linked into the capture path.

[dependencies]
bincode = "1.3"
triton-vm = "7.0"
