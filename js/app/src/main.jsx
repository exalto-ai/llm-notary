import { useEffect, useMemo, useRef, useState } from 'react';
import { createRoot } from 'react-dom/client';
import './styles.css';
import './refinements.css';
import './commons.css';
import './theme.css';
import './branding.css';
import './account.css';

const records = [
  { id: 'refusal', title: 'Refusal boundary test', api: 'OpenAI · Responses API', provider: 'OpenAI', host: 'api.openai.com', model: 'gpt-4.1', date: 'Jul 24, 2026', sortDate: 20260724, records: '42 turns', hash: 'a47e32…ef90', tags: ['Safety', 'Refusals'], license: 'CC BY 4.0', summary: 'A redacted test of a model refusal boundary. The receipt preserves the provider response and model identifier.' },
  { id: 'tool', title: 'Tool-use evaluation', api: 'Anthropic · Messages API', provider: 'Anthropic', host: 'api.anthropic.com', model: 'claude-sonnet-4', date: 'Jul 23, 2026', sortDate: 20260723, records: '18 turns', hash: 'c18b70…79aa', tags: ['Evaluation', 'Tools'], license: 'CC BY 4.0', summary: 'A redacted tool call and final response. Private runtime context is removed before sharing.' },
  { id: 'benchmark', title: 'Benchmark run #048', api: 'OpenAI · Chat Completions', provider: 'OpenAI', host: 'api.openai.com', model: 'gpt-4.1-mini', date: 'Jul 19, 2026', sortDate: 20260719, records: '200 prompts', hash: 'f10cd2…05b7', tags: ['Research', 'Benchmark'], license: 'CC BY 4.0', summary: 'A prompt-and-response record from an instruction-following benchmark, published with its origin receipt.' },
  { id: 'context', title: 'Long-context comparison', api: 'Anthropic · Messages API', provider: 'Anthropic', host: 'api.anthropic.com', model: 'claude-opus-4', date: 'Jul 16, 2026', sortDate: 20260716, records: '24 runs', hash: 'd49186…3c4d', tags: ['Research', 'Long context'], license: 'CC BY 4.0', summary: 'A redacted comparison run that records a long-context prompt and the provider’s returned response.' },
  { id: 'coding', title: 'Patch review prompts', api: 'OpenAI · Responses API', provider: 'OpenAI', host: 'api.openai.com', model: 'gpt-4.1-mini', date: 'Jul 11, 2026', sortDate: 20260711, records: '86 prompts', hash: '0c852e…a14f', tags: ['Coding', 'Evaluation'], license: 'CC BY 4.0', summary: 'A collection of redacted code-review prompts and provider responses, accompanied by verifiable receipts.' },
];

const installCommand = 'curl -fsSLO https://llmnotary.exalto.ai/install.sh && sh install.sh';
const brandAssetVersion = __BRAND_ASSET_VERSION__;

function PenMark({ inverse = false }) {
  return <span className={`pen-mark${inverse ? ' pen-mark--inverse' : ''}`} aria-hidden="true">{inverse ? <img src={`/logo-light.png?v=${brandAssetVersion}`} alt="" /> : <picture><source media="(prefers-color-scheme: dark)" srcSet={`/logo-light.png?v=${brandAssetVersion}`} /><img src={`/logo-dark.png?v=${brandAssetVersion}`} alt="" /></picture>}</span>;
}

function CloseIcon() { return <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M6 6l12 12M18 6 6 18"/></svg>; }
function Arrow() { return <div className="flow-arrow" aria-label="Encrypted connection"><span>encrypted</span><svg viewBox="0 0 68 14" preserveAspectRatio="none" aria-hidden="true"><path d="M1 7h61M56 2l6 5-6 5"/></svg></div>; }
function FlowNode({ type, title, note }) { return <div className={`flow-node flow-node--${type}`}><span className="node-mark" aria-hidden="true"/><strong>{title}</strong><small>{note}</small></div>; }

function AccountMenu({ user, onLogout }) {
  const [open, setOpen] = useState(false);
  const menuRef = useRef(null);
  const initials = user.github_login.slice(0, 2).toUpperCase();
  useEffect(() => {
    const close = (event) => {
      if (event.key === 'Escape' || (event.type === 'mousedown' && !menuRef.current?.contains(event.target))) setOpen(false);
    };
    document.addEventListener('mousedown', close);
    window.addEventListener('keydown', close);
    return () => { document.removeEventListener('mousedown', close); window.removeEventListener('keydown', close); };
  }, []);
  return <div className="account-menu" ref={menuRef}><button type="button" className="account-trigger" onClick={() => setOpen((value) => !value)} aria-expanded={open} aria-haspopup="menu" aria-label={`Account menu for ${user.github_login}`}>{user.avatar_url ? <img src={user.avatar_url} alt="" referrerPolicy="no-referrer" /> : <span>{initials}</span>}</button>{open && <div className="account-popover" role="menu"><div className="account-identity"><b>{user.github_login}</b><span>Signed in with GitHub</span></div><a href="#/dashboard" role="menuitem" onClick={() => setOpen(false)}>Dashboard</a><button type="button" role="menuitem" onClick={() => { setOpen(false); onLogout(); }}>Log out</button></div>}</div>;
}

function Header({ user, onLogout }) {
  return <header className="nav-wrap"><a className="brand" href="#/"><PenMark /> <span>LLM Notary</span></a><nav className="product-nav"><a href="#/docs">Docs</a><a href="#/collections">Collections</a>{user ? <AccountMenu user={user} onLogout={onLogout} /> : <a className="sign-in-link" href="/api/auth/github">Sign in</a>}</nav></header>;
}

function Footer() {
  return <footer className="site-footer"><a className="brand" href="#/"><PenMark /> <span>LLM Notary</span></a><span>Verifiable traces for AI research and evaluation.</span><span>© 2026 LLM Notary</span></footer>;
}

function Diagram() {
  return <div className="diagram-card"><div className="diagram-label">One model call</div><div className="flow" aria-label="Your machine connects through LLM Notary to the AI provider"><FlowNode type="local" title="Your machine" note="Local proxy" /><Arrow /><FlowNode type="notary" title="LLM Notary" note="TLS witness" /><Arrow /><FlowNode type="provider" title="AI provider" note="OpenAI · Anthropic · DeepSeek" /></div><div className="diagram-foot">The notary relays encrypted TLS traffic. It cannot read your API key, prompt, or response.</div></div>;
}

function VerifierDialog({ onClose }) {
  const [fileName, setFileName] = useState('');
  useEffect(() => { const close = (event) => event.key === 'Escape' && onClose(); window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close); }, [onClose]);
  return <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><section className="verifier-modal" role="dialog" aria-modal="true" aria-labelledby="verifier-title" onMouseDown={(event) => event.stopPropagation()}><button className="icon-button" onClick={onClose} aria-label="Close verifier"><CloseIcon /></button><span className="eyebrow">Verifier preview</span><h2 id="verifier-title">Inspect a trace</h2><p>The command-line verifier is available now. This browser flow previews the local file experience.</p><label className="drop-zone"><input type="file" accept=".json,.notary" onChange={(event) => setFileName(event.target.files?.[0]?.name || '')} /><strong>{fileName || 'Choose a receipt file'}</strong><small>{fileName ? 'Selected locally' : 'JSON or .notary'}</small></label><div className="verification-state"><i /> Use <code>llm-notary verify</code> for a cryptographic check</div></section></div>;
}

function Landing({ onVerify }) {
  return <main id="top"><section className="hero"><span className="eyebrow">LLM Notary</span><h1>Share evidence of<br />how models behave.</h1><p>Keep model calls on your machine. Retain a trace package with a receipt that others can independently verify.</p><div className="hero-actions"><a className="button button-dark" href="#/docs/install">Install the CLI</a><a className="button button-plain" href="#/collections">Browse Collections</a></div><div className="hero-metadata"><span>For research, evaluation, and safety work</span><b /><span>Local capture. Deliberate sharing.</span></div></section><section className="section architecture" id="how-it-works"><div className="section-head"><span className="eyebrow">How it works</span><h2>Capture locally. Verify anywhere.</h2></div><div className="architecture-grid"><Diagram /><div className="step-list"><article><span>01</span><div><h3>Run the local proxy</h3><p>Your SDK keeps its normal API shape while the proxy saves a trace package on your machine.</p></div></article><article><span>02</span><div><h3>Keep the evidence</h3><p>The notary signs evidence that the response came from the named provider connection.</p></div></article><article><span>03</span><div><h3>Share a chosen trace</h3><p>Review redactions and reuse terms before adding work to a research or evaluation collection.</p></div></article></div></div><div className="section-link"><a href="#/docs/how-it-works">Read how the trust model works</a></div></section><section className="privacy"><div><span className="eyebrow">Privacy by default</span><h2>Evidence without handing over your work.</h2></div><p>Captures stay local until you choose otherwise. The public Collections are for intentionally shared, redacted records with clear reuse terms.</p></section><section className="section library-preview"><div className="trace-heading"><div><span className="eyebrow">Collections</span><h2>Trace records with useful context.</h2></div><p>Searchable by provider, model, and topic. The records below are illustrative while the Collections service is being built.</p></div><div className="preview-records">{records.slice(0, 3).map((record) => <article key={record.id}><i aria-hidden="true"/><div><h3>{record.title}</h3><p>{record.provider} · {record.model}</p></div><span>{record.tags[0]}</span></article>)}</div><a className="button button-dark" href="#/collections">Open Collections</a></section><section className="section verify" id="verify"><div><span className="eyebrow">Independent verification</span><h2>Inspect before you use it.</h2><p>Check a trace package and the trusted notary key locally. No account or upload is required for the CLI verifier.</p><div className="verify-points"><span>No account</span><span>No upload</span><span>Open format</span></div><div className="button-row"><a className="button button-dark" href="#/docs/verify">Verify with the CLI</a><button className="button button-plain" onClick={onVerify}>Preview browser flow</button></div></div><div className="receipt"><header><PenMark inverse /><b>Trace package</b></header><h3>Receipt</h3><dl><div><dt>Provider</dt><dd>api.openai.com</dd></div><div><dt>Model</dt><dd>gpt-4.1</dd></div><div><dt>Evidence</dt><dd>TLSNotary presentation</dd></div><div><dt>Reuse</dt><dd>CC BY 4.0</dd></div></dl><footer>LLM NOTARY / v0.1</footer></div></section><section className="section install"><div><span className="eyebrow">Get started</span><h2>Install once.<br />Keep using your tools.</h2></div><div className="terminal"><div><i /><i /><i /></div><pre><code><b>$</b> {installCommand}{'\n\n'}<b>$</b> llm-notary proxy start --provider openai{'\n\n'}listening  <em>127.0.0.1:8787</em>{'\n'}ready</code></pre><a href="#/docs/install">Installation and setup</a></div></section></main>;
}

const docPages = {
  overview: { title: 'LLM Notary documentation', lead: 'Capture provider-origin model traces locally, then check the attached TLSNotary receipt without asking an application to vouch for it.', blocks: [['What is in a local capture?', 'The proxy writes a directory with a manifest, TLSNotary evidence, a selectively disclosed request, and the authenticated provider response. Authorization values are never saved.'], ['What a receipt proves', 'A valid receipt ties the disclosed response to a TLS connection to the provider hostname and to the notary signing key selected by the verifier.'], ['Current scope', 'OpenAI, Anthropic, and DeepSeek HTTP/1.1 POST API calls are supported. Streaming responses are saved after their final event.']] },
  'how-it-works': { title: 'How the trust model works', lead: 'LLM Notary separates the provider connection, local plaintext, and the attestation signer so a client cannot simply invent provider output.', blocks: [['1. The proxy stays local', 'Your API key, prompt, and response are handled by the local proxy. The proxy uses your ordinary provider-compatible client.'], ['2. The notary witnesses encrypted traffic', 'The remote notary opens the allowlisted provider connection and relays encrypted TLS records. It does not receive your API key or plaintext transcript.'], ['3. The verifier chooses a trust root', 'Verification is local. The recipient supplies the expected notary public key, then checks the TLSNotary presentation and disclosed hashes.']] },
  install: { title: 'Install the CLI', lead: 'The installer downloads a checksum-verified release for macOS or Linux. Windows releases are available as ZIP archives.', blocks: [['Recommended install', installCommand], ['Supported packages', 'macOS: Apple silicon and Intel. Linux: x86_64 and ARM64. Windows: x86_64 ZIP release. Each archive contains the llm-notary command.'], ['Choose a version', 'Set LLM_NOTARY_VERSION before running the installer to select a specific release. Set LLM_NOTARY_INSTALL_DIR to install somewhere other than ~/.local/bin.']] },
  proxy: { title: 'Run the local proxy', lead: 'Start the proxy, point an OpenAI-compatible client to it, and keep your existing provider API key in that client.', blocks: [['Start it', 'llm-notary proxy start --provider openai --capture-dir captures'], ['Connect your client', 'Use http://127.0.0.1:8787/v1 as the base URL. DeepSeek uses http://127.0.0.1:8787 without a /v1 suffix. The proxy writes each completed capture to the configured directory.'], ['Providers', 'Use --provider openai, --provider anthropic, or --provider deepseek. The current notary allowlist only accepts their API hostnames.']] },
  verify: { title: 'Verify a local capture', lead: 'Verification runs locally. The verifier reads a capture directory and checks it against the public key you explicitly trust.', blocks: [['Check a capture', 'llm-notary verify captures/cap-... --trusted-notary-key <notary-public-key>'], ['Summary only', 'Add --summary to check the receipt and hashes without printing the disclosed request and response.'], ['What to inspect', 'Confirm the provider hostname, model details in the disclosed response, and the selected notary public key before relying on a trace.']] },
};

function Docs({ pageKey }) {
  const page = docPages[pageKey] || docPages.overview;
  const isCommand = (text) => text.startsWith('curl ') || text.startsWith('llm-notary ');
  return <main className="docs-shell"><aside className="docs-sidebar"><span className="eyebrow">Documentation</span><a className={!pageKey || pageKey === 'overview' ? 'active' : ''} href="#/docs">Overview</a><a className={pageKey === 'how-it-works' ? 'active' : ''} href="#/docs/how-it-works">How it works</a><span className="docs-group">CLI</span><a className={pageKey === 'install' ? 'active' : ''} href="#/docs/install">Install</a><a className={pageKey === 'proxy' ? 'active' : ''} href="#/docs/proxy">Proxy</a><a className={pageKey === 'verify' ? 'active' : ''} href="#/docs/verify">Verify</a><a href="#/collections">Collections</a></aside><article className="docs-content"><span className="eyebrow">LLM Notary / Docs</span><h1>{page.title}</h1><p className="docs-lead">{page.lead}</p>{page.blocks.map(([heading, body]) => <section key={heading}><h2>{heading}</h2>{isCommand(body) ? <pre><code>{body}</code></pre> : <p>{body}</p>}</section>)}<div className="docs-next"><span>Next</span><a href={pageKey === 'verify' ? '#/collections' : '#/docs/proxy'}>{pageKey === 'verify' ? 'Browse Collections' : 'Run the local proxy'}</a></div></article></main>;
}

function Collections({ onVerify }) {
  const [query, setQuery] = useState(''); const [provider, setProvider] = useState('All'); const [model, setModel] = useState('All'); const [tag, setTag] = useState('All'); const [sort, setSort] = useState('Newest'); const [active, setActive] = useState(records[0]);
  const providers = ['All', ...new Set(records.map((record) => record.provider))]; const models = ['All', ...new Set(records.map((record) => record.model))]; const tags = ['All', ...new Set(records.flatMap((record) => record.tags))];
  const filtered = useMemo(() => records.filter((record) => { const searchable = `${record.title} ${record.provider} ${record.model} ${record.tags.join(' ')} ${record.summary}`.toLowerCase(); return searchable.includes(query.toLowerCase()) && (provider === 'All' || record.provider === provider) && (model === 'All' || record.model === model) && (tag === 'All' || record.tags.includes(tag)); }).sort((a, b) => sort === 'Newest' ? b.sortDate - a.sortDate : a.title.localeCompare(b.title)), [query, provider, model, tag, sort]);
  useEffect(() => { if (filtered.length && !filtered.some((record) => record.id === active.id)) setActive(filtered[0]); }, [filtered, active]);
  return <main className="library-shell"><section className="library-hero"><span className="eyebrow">Collections</span><h1>Verifiable traces,<br />ready to inspect.</h1><p>Explore shared records by provider, model, and research topic. Every item is designed to carry its own evidence and reuse context.</p></section><section className="library-controls" aria-label="Collection filters"><label className="library-search"><span>Search</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Model, provider, topic" /></label><label><span>Provider</span><select value={provider} onChange={(event) => setProvider(event.target.value)}>{providers.map((value) => <option key={value}>{value}</option>)}</select></label><label><span>Model</span><select value={model} onChange={(event) => setModel(event.target.value)}>{models.map((value) => <option key={value}>{value}</option>)}</select></label><label><span>Sort</span><select value={sort} onChange={(event) => setSort(event.target.value)}><option>Newest</option><option>Title</option></select></label></section><section className="library-results"><aside className="tag-filter"><span>Topics</span>{tags.map((value) => <button key={value} className={tag === value ? 'active' : ''} onClick={() => setTag(value)}>{value}</button>)}</aside><div><div className="results-heading"><p><b>{filtered.length}</b> illustrative records</p><span>Collection publishing is not enabled yet.</span></div><div className="library-grid">{filtered.map((record) => <button className={`library-card${active.id === record.id ? ' active' : ''}`} key={record.id} onClick={() => setActive(record)}><div><i aria-hidden="true"/><span>{record.provider}</span><time>{record.date}</time></div><h2>{record.title}</h2><p>{record.model} · {record.records}</p><div className="tag-list">{record.tags.map((item) => <span key={item}>{item}</span>)}</div></button>)}</div></div><article className="library-detail"><div><span className="eyebrow">Selected record</span><h2>{active.title}</h2><p>{active.summary}</p></div><dl><div><dt>Provider</dt><dd>{active.host}</dd></div><div><dt>Model</dt><dd>{active.model}</dd></div><div><dt>Package</dt><dd>ln_{active.id}_{active.hash}</dd></div><div><dt>Reuse</dt><dd>{active.license}</dd></div></dl><div className="trace-actions"><button onClick={onVerify}>Inspect receipt</button><a href="#/docs/verify">Verification docs</a></div></article></section></main>;
}

function Dashboard({ user }) {
  return <main className="dashboard-shell"><span className="eyebrow">Account</span><h1>Welcome, {user.github_login}.</h1><p>Your LLM Notary account is ready. Trace publishing and API keys will appear here as those services come online.</p><div className="dashboard-card"><span>Signed in with GitHub</span><b>{user.github_login}</b><a href="#/collections">Browse Collections</a></div></main>;
}

function App() {
  const [route, setRoute] = useState(window.location.hash || '#/'); const [showVerifier, setShowVerifier] = useState(false); const [user, setUser] = useState(null);
  useEffect(() => { const update = () => setRoute(window.location.hash || '#/'); window.addEventListener('hashchange', update); return () => window.removeEventListener('hashchange', update); }, []);
  useEffect(() => { let cancelled = false; fetch('/api/me', { credentials: 'same-origin' }).then((response) => response.ok ? response.json() : null).then((payload) => { if (!cancelled) setUser(payload?.user || null); }).catch(() => { if (!cancelled) setUser(null); }); return () => { cancelled = true; }; }, []);
  const logout = async () => { const response = await fetch('/api/auth/logout', { method: 'POST', credentials: 'same-origin' }); if (response.ok) { setUser(null); if (window.location.hash === '#/dashboard') window.location.hash = '#/'; } };
  const path = route.replace(/^#\/?/, ''); const [section, page] = path.split('/');
  return <><Header user={user} onLogout={logout} />{section === 'docs' ? <Docs pageKey={page || 'overview'} /> : (section === 'collections' || section === 'library') ? <Collections onVerify={() => setShowVerifier(true)} /> : section === 'dashboard' && user ? <Dashboard user={user} /> : <Landing onVerify={() => setShowVerifier(true)} />}<Footer />{showVerifier && <VerifierDialog onClose={() => setShowVerifier(false)} />}</>;
}

createRoot(document.getElementById('root')).render(<App />);
