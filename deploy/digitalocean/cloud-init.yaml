#cloud-config
package_update: true
packages:
  - ca-certificates
  - docker.io
  - docker-compose-v2
  - openssl

runcmd:
  - mkdir -p /opt/llm-notary
  - umask 077 && openssl rand -hex 32 > /opt/llm-notary/notary-signing-key
