#cloud-config
package_update: true
packages:
  - ca-certificates
  - docker.io
  - git
  - openssl

runcmd:
  - mkdir -p /opt/certified
  - umask 077 && openssl rand -hex 32 > /opt/certified/notary-signing-key
